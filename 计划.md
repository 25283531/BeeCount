**实施计划**
- 目标：在“智能记账”中新增“语音记账”，实现端侧语音转文字 + 现有解析与记账落库，全流程离线可用。
- ASR优先方案：先集成 Vosk（轻量、本地离线），后续可选 Whisper.cpp（高精度，FFI 更复杂）。
- 解析与落库：复用现有 `parsePaymentText` + `AIBillService` 的增强逻辑与 `BillCreationService.createBillTransaction` 创建交易。
- UI：新增 `VoiceBillingPage`，在“智能记账”页添加入口卡片，与 OCR 流程一致的确认与创建体验。

**改动范围**
- 依赖与模型
  - 在 `pubspec.yaml:49` 引入 `vosk_flutter`（或同类端侧 ASR 插件），保留已有 `permission_handler` 用于麦克风权限。
  - 管理 Vosk 模型包下载与解压（首次使用时），模型存放于应用私有目录，结合“存储管理”页面进行清理。
- 新增页面与服务
  - 新增 `lib/pages/automation/voice_billing_page.dart`：录音控制、波形/状态、识别完成回显文本、解析与确认。
  - 新增 `lib/services/automation/asr_service.dart`：封装 Vosk 初始化、流式识别、资源释放。
- 现有解析与创建的对接
  - 文本到结构化解析：调用 `OcrService.parsePaymentText(rawText)`（`lib/services/automation/ocr_service.dart:268`）。
  - 可选 AI 增强：调用 `AIBillService.extractBillInfo`（`lib/services/automation/ai_bill_service.dart:82`），受用户策略控制：
    - 策略读取与展示 `lib/pages/ai/ai_settings_page.dart:43, 188`（`ai_strategy` 与 `ai_bill_extraction_enabled`）。
    - 执行上下文本地化控制 `packages/flutter_ai_kit/src/core/ai_execution_context.dart:41`。
  - 创建交易：`BillCreationService.createBillTransaction(...)`（`lib/services/automation/bill_creation_service.dart:155`）。
- UI入口与文案
  - 在“智能记账”页添加入口卡片（与 OCR 并列）：`lib/pages/settings/smart_billing_page.dart:69`。
  - 本地化字符串在 `l10n` 增补“语音记账”、提示与错误文案（遵循现有 `arb`/`generated` 机制）。

**实现步骤**
- 第 1 步：ASR 基建
  - 引入 `vosk_flutter`，实现 `AsrService`：初始化模型、请求麦克风权限、开始/停止录音、输出识别文本。
  - 首次使用时下载或导入合适语言模型并解压到私有目录；在“存储管理”提供清理入口（参考 `lib/pages/settings/storage_management_page.dart:140` 的清理流程）。
- 第 2 步：语音记账页面
  - `VoiceBillingPage`：开始录音/结束录音，状态文案与轻量波形；识别文本展示与编辑；点击“解析”走解析流程。
  - 解析流程：先 `parsePaymentText` 提取金额/商家/时间，再依据用户 `ai_strategy` 决定是否调用 `AIBillService` 做增强；增强后进入与 OCR 同样的确认界面与“创建账单”按钮（参考 `lib/pages/automation/ocr_billing_page.dart:409, 560`）。
- 第 3 步：创建交易与刷新
  - 调用 `BillCreationService.createBillTransaction` 落库；刷新统计与小组件，参考 OCR 流程的刷新（`lib/pages/transaction/transaction_editor_page.dart:221`）。
- 第 4 步：策略与隐私
  - 当策略为 `local_only` 或无网络时，强制 `AIExecutionContext.localOnly(...)`，不访问云端 Provider。
  - 语音与临时文本仅在本地处理，识别后删除临时音频文件。
- 第 5 步：本地化与提示
  - 增加“语音记账”标题、副标题、提示/错误文案到 `arb`，更新生成的 `app_localizations_xx.dart`。
- 第 6 步：测试与验证
  - 单元：以中文常见语句（如“今天买咖啡花了30元”）验证 `parsePaymentText` 金额/时间提取。
  - 集成：语音→识别→解析→确认→创建交易端到端流程；不同策略（`local_first`/`local_only`）下验证增强是否参与。
  - 运行检查：在完成实现后运行格式检查与类型检查（例如 `flutter analyze`、`flutter test`），并做必要修复。

**风险与兼容**
- 模型体积与设备性能：Vosk 轻量但仍需较大模型；提供下载与清理、低端设备提示。
- Whisper.cpp 集成复杂度高：作为第二阶段可选项，通过 FFI 暴露接口，保留 Vosk 为默认。
- 本地 NER 模型目前为占位（`lib/ai/providers/bill_extraction_tflite_provider.dart:62` 提示未训练）；短期以规则 + 云端增强（在非 `local_only` 且用户启用时）保证体验。

**后续可选增强**
- TFLite NER 模型训练与部署，替代占位实现，提升 `local_only` 下的解析准确率。
- 语音意图短语适配（“星巴克消费”“滴滴打车”）与分类词库强化。
- 录音与识别的进度动画与错误恢复。
----end
